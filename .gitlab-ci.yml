image: python:3.6.8

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - env/

before_script:
  - python -V
  - ./install_requirements_dev.sh

test:
  script:
    - mypy .
    - pytest
